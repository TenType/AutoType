{
  "name": "autotype",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "postinstall": "npm run build",
    "build": "tsc",
    "start": "node .",
    "deploy:dev": "NODE_ENV=development node dist/deploy-commands.js",
    "deploy:prod": "NODE_ENV=production node dist/deploy-commands.js",
    "dev": "NODE_ENV=development npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "TenType",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "discord.js": "^14.6.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "typescript": "^4.8.4"
  }
}
